{
  "name": "investment-intelligence-mcp",
  "version": "1.0.0",
  "private": true,
  "description": "Modular investment intelligence MCP with standalone source tools and cross-source research workflows",
  "license": "MIT",
  "engines": {"node": ">=20"},
  "scripts": {
    "check": "node --check api/mcp.js && node --check api/health.js && node --check lib/mcp-client.js && node --check lib/tools.js && node --check lib/analysis.js",
    "test": "node --test tests/*.test.js",
    "build": "npm run check"
  }
}
